import { PieceType, Side, Piece, Position, samePostion } from "../Constants";
import {
  pawnMove,
  knightMove,
  bishopMove,
  rookMove,
  kingMove,
  getAllPawnMoves,
  getAllKnightMoves,
  getAllBishopMoves,
  getAllRookMoves,
  getAllKingMoves,
} from "./rules";

export default class Rules {
  

}
